/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ["res.cloudinary.com", "images.unsplash.com","img.freepik.com","'avatars.githubusercontent.com'"],
  },
};

export default nextConfig;